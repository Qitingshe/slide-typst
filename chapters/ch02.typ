// chapters/ch02.typ - 第 2 章 上下文工程
// 强调色：framavert（继承自 ch01）；结尾交给 ch03 为 framaviolet。
#import "../lib.typ": *

// ---------- 第 1 页 · 强调色 framavert（继承）----------

== 第 2 章 · 上下文的结构

#keyline[上下文 = 静态前缀 + 轨迹]

#v(0.3em)

#grid(
  columns: (1fr, 1fr),
  gutter: 1em,
  [
    #boiteverte[
      #align(center)[*静态前缀*]
      - 系统提示词：身份 / 权限 / 行为准则
      - 工具定义：名称 / 描述 / 参数格式
    ],
  ],
  [
    #boiteorange[
      #align(center)[*动态轨迹*]
      - 用户消息（可注入 RAG 知识）
      - 模型回复 + 工具执行结果
    ],
  ],
)

#v(0.4em)

#boitebleue[*KV Cache* 缓存已见 token 的键值，避免重复预填充——静态前缀越长，缓存收益越大]

#v(0.25em)

#note[上下文 = 每次调用拼接后的完整输入]

// ---------- 第 2 页 · 强调色 framavert ----------

== 第 2 章 · 从提示工程到上下文工程

#keyline[上下文决定能力上限：系统性地管理模型看到的一切]

#v(0.3em)

- 从「怎么问」到「让模型看到什么」：上下文质量优先于措辞技巧
- 优先级与令牌预算：什么信息该进、哪些该剪
- Agent Skills：把可复用能力封装成带教程与示例的技能包
- Sidecar 旁路查询：不撑爆主窗口，按需注入外部信息

#v(0.4em)

#note[提示工程 ⊂ 上下文工程]

// ---------- 第 3 页 · 强调色 framavert ----------

== 第 2 章 · 长程任务与压缩

#keyline[轨迹越长，越需要工程化治理]

#v(0.3em)

- 上下文压缩三路径：摘要 / 丢弃 / 检索注入
- 长程任务：数小时的自主运行需要令牌预算与记忆管理
- 语境与遗忘：旧信息何时该让位——衔接第 3 章记忆

#v(0.3em)

#boitefilled[上下文是根：一切 Agent 能力都长在它上面]

// 下一页强调色：本页末尾收尾声明（衔接 ch03）
#slide-accent(framaviolet)